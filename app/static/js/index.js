'use strict';

(function($) {

	var index = 0;
	var headersEN = [
		'a digital media designer',
		'a full-stack web developer',
		'a photographer',
		'a copywriter &amp; author',
		'an online marketing expert'
	];

	var changeText = function(jqObj, headers) {
		var removeLetter = function() {
			if(jqObj.html() === '') {
				addLetter();
			}
		}
	};

})(jQuery);